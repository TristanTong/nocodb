# 100.93 生产升级到 v0.1.6（打包与切换）

| 项 | 内容 |
|----|------|
| 目标主机 | `192.168.100.93` |
| **升级结果（2026-09-03）** | **已完成**：`mlnocodb:0.1.6` / API **0.301.3**；`mssql_ok`；健康检查 OK |
| 断机窗口 | compose recreate ≈1s；服务就绪约 **~10s**（仅 API；nginx/ui 未动） |
| 当前回滚点 | `/opt/mlnocodb/backup-pre-0.1.6/` + 镜像 tag `mlnocodb:0.1.1-pre-0.1.6` |
| Meta | `192.168.100.97:5432` / `mlnoco`（已手工备份，升级未改库机） |
| 共机 | Dify 等未重启 |

关联：`scripts/compat/upgrade_prod93_to_0.1.6.py`、`build_0.1.6_commit_on_89.py`。

---

## 升级已执行摘要

| 阶段 | 状态 |
|------|------|
| 不停服：备份 compose + `docker save` 旧镜像 242M | ✅ |
| 不停服：打 `mlnocodb:0.1.1-pre-0.1.6` | ✅ |
| 不停服：89 提交构建 `0.1.6`（基线 `0.1.3`+新 `main.js`）→ 93 `docker load` | ✅ |
| 短切：compose `api` → `mlnocodb:0.1.6`，`--force-recreate --no-deps api` | ✅ |
| 验收：`/api/v1/version`=`0.301.3`，health OK，`mssql_ok` | ✅ |

### 回滚（若需）

```bash
cd /opt/mlnocodb
cp /opt/mlnocodb/backup-pre-0.1.6/docker-compose.yml ./docker-compose.yml
# 或: sed -i 's#image: mlnocodb:.*#image: mlnocodb:0.1.1-pre-0.1.6#' docker-compose.yml
docker compose up -d --force-recreate --no-deps api
# 若本地无旧层: gunzip -c /opt/mlnocodb/backup-pre-0.1.6/mlnocodb-0.1.1-pre-0.1.6.tar.gz | docker load
```

---

## 构建说明（本次实际）

因内网 Docker build 阶段 `npm install mssql` 失败，采用：

```bash
# 在 100.89：基于已含 mssql 的 mlnocodb:0.1.3
python scripts/compat/build_0.1.6_commit_on_89.py
# → docker commit → mlnocodb:0.1.6 → /opt/mlnocodb/mlnocodb-0.1.6.tar.gz
```

标准 Dockerfile 路径仍见 `package_release_0.1.6.py`（需可达 npm）。

---

## 前端补丁（2026-09-03）：Integrations 无 SQL Server

**原因**：只升了 API（`mlnocodb:0.1.6`），`/opt/mlnocodb/ui` 仍是 **2026-07-23** 构建，包内无 `objects.syncData.mssql`；测试机 89 的 `ui-output`（**2026-07-24**）已含 SQL Server 选项。

**处理**：将 89 `ui-output` 同步到 93 `/opt/mlnocodb/ui`（先备份到 `backup-pre-0.1.6/ui-pre-0.1.6.tgz`），仅 recreate `mlnocodb-ui`。脚本：`scripts/compat/sync_ui_mssql_89_to_93.py`。

请浏览器 **强制刷新**（Ctrl+F5）后再打开 Integrations。

## 升级后跟进（非阻塞）

1. Meta `nc_order`：Decimal/Number → Order（97）
2. 业务 Base / SQL Server 冒烟
3. 观察 24h 内存与 Dify 同机稳定性
